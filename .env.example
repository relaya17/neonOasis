# ============ API (apps/api) ============
NODE_ENV=development
PORT=4000
API_URL=http://localhost:4000

# PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/neon_oasis

# Redis (sync, leaderboards, tournaments)
REDIS_URL=redis://localhost:6379

# Socket.io / CORS
CORS_ORIGIN=http://localhost:5273

# Admin API (optional): אם מוגדר — כל קריאה ל-/api/admin/* דורשת header X-Admin-Secret
# ADMIN_SECRET=your-secret-here

# RNG (Provably Fair): secret for HMAC; set in production
# RNG_SECRET=your-random-secret-32-chars

# AI / Safety (optional for MVP)
# ONFIDO_API_KEY=
# TFJS_MODEL_PATH=

# ============ Web (apps/web) ============
# Vite injects VITE_* at build
VITE_API_URL=http://localhost:4000
VITE_WS_URL=ws://localhost:4000
VITE_SOCKET_URL=http://localhost:4000

# וידאו — נתיבים חיצוניים (Cloudinary). אם לא מוגדר — משתמשים בברירת מחדל בקוד.
# VITE_VIDEO_INTRO_URL=https://res.cloudinary.com/dora8sxcb/video/upload/v1770403442/all.png_w8jliq.mp4
# VITE_VIDEO_POKER_INTRO_URL=    # פוקר
# VITE_VIDEO_BACKGAMMON_INTRO_URL=  # שש-בש (ברירת מחדל: Cloudinary)
# VITE_VIDEO_LEADERBOARD_URL=    # רקע לוח מובילים
# VITE_VIDEO_SNOOKER_INTRO_URL=  # וידאו כרטיס סנוקר (בדף בחירת משחק)
# VITE_VIDEO_TOUCH_INTRO_URL=    # וידאו כרטיס טאצ'/סוליטר (בדף בחירת משחק)
