# ============ API (apps/api) ============
NODE_ENV=development
PORT=4000
API_URL=http://localhost:4000

# PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/neon_oasis

# Redis (sync, leaderboards, tournaments)
REDIS_URL=redis://localhost:6379

# Socket.io / CORS
CORS_ORIGIN=http://localhost:5273

# Admin API (optional): אם מוגדר — כל קריאה ל-/api/admin/* דורשת header X-Admin-Secret
# ADMIN_SECRET=your-secret-here

# RNG (Provably Fair): secret for HMAC; set in production
# RNG_SECRET=your-random-secret-32-chars

# AI / Safety (optional for MVP)
# ONFIDO_API_KEY=
# TFJS_MODEL_PATH=

# ============ Web (apps/web) ============
# Vite injects VITE_* at build
VITE_WS_URL=ws://localhost:4000
VITE_API_URL=http://localhost:4000
