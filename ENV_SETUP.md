# 🔐 Environment Variables Setup

## 📍 Local Development (.env)

כבר קיים: `.env` בתיקיית הראשית

```bash
# API
NODE_ENV=development
PORT=4000
CORS_ORIGIN=http://localhost:5273

# Web
VITE_API_URL=http://localhost:4000
VITE_WS_URL=ws://localhost:4000
VITE_SOCKET_URL=http://localhost:4000

# וידאו מהענן (אופציונלי — אם לא מוגדר משתמשים בקבצים מ-public/images)
# VITE_VIDEO_INTRO_URL=https://res.cloudinary.com/your-cloud/video/upload/xxx/all.mp4
# VITE_VIDEO_POKER_INTRO_URL=https://res.cloudinary.com/your-cloud/video/upload/xxx/poker.mp4
# VITE_VIDEO_BACKGAMMON_INTRO_URL=https://res.cloudinary.com/your-cloud/video/upload/xxx/play1.mp4
```

---

## ☁️ Vercel (Production - Web)

### Environment Variables to Add:

```bash
VITE_API_URL=https://neon-oasis-api.onrender.com
VITE_WS_URL=wss://neon-oasis-api.onrender.com
VITE_SOCKET_URL=https://neon-oasis-api.onrender.com
```

**איפה להוסיף:**
1. Vercel Dashboard → Your Project
2. Settings → Environment Variables
3. Add each variable for **Production, Preview, Development**

---

## ☁️ Render (Production - API)

### Environment Variables to Add:

#### Required:
```bash
NODE_ENV=production
PORT=4000
CORS_ORIGIN=https://your-app.vercel.app
```

#### Database (Auto-generated by Render):
```bash
DATABASE_URL=<render-will-provide-this>
```

#### Redis (Optional, Auto-generated):
```bash
REDIS_URL=<render-will-provide-this>
```

#### Optional Features:
```bash
# Admin API Protection
ADMIN_SECRET=your-super-secret-key-here

# RNG Secret (for provably fair)
RNG_SECRET=random-32-character-secret-key
```

**איפה להוסיף:**
1. Render Dashboard → Your Service
2. Environment → Add Environment Variable
3. Save Changes

---

## 🔄 Update Flow

### כשמשנים משתני סביבה:

**Vercel:**
1. Dashboard → Settings → Environment Variables
2. עדכן ערך
3. Redeploy (אוטומטי או ידני)

**Render:**
1. Dashboard → Environment
2. עדכן ערך
3. שמור → Auto redeploy

---

## ✅ Checklist

### Local Development:
- [x] `.env` קיים
- [x] `VITE_API_URL` מצביע ל-localhost:4000
- [x] `CORS_ORIGIN` מאפשר localhost:5273

### Vercel:
- [ ] `VITE_API_URL` מצביע ל-Render API URL
- [ ] `VITE_WS_URL` מצביע ל-Render API (wss://)
- [ ] כל המשתנים מוגדרים ל-Production

### Render:
- [ ] `CORS_ORIGIN` כולל את Vercel URL
- [ ] `DATABASE_URL` מחובר לRender PostgreSQL
- [ ] `PORT` = 4000 (או השתמש ב-process.env.PORT)
- [ ] Schema הורץ על הDB

---

## 🔐 Security Notes

### לא לשתף:
- ❌ `DATABASE_URL` (sensitive!)
- ❌ `REDIS_URL` (sensitive!)
- ❌ `ADMIN_SECRET` (if set)
- ❌ `RNG_SECRET` (if set)

### בטוח לשתף:
- ✅ `VITE_API_URL` (public)
- ✅ `VITE_WS_URL` (public)
- ✅ `CORS_ORIGIN` (public)

---

**Environment Variables מוכנות! 🎯**
